<UserControl x:Class="WPFUI_NEW.Views.ClientConnectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:WPFUI_NEW.ViewModels"
             xmlns:converters="clr-namespace:WPFUI_NEW.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance vm:ClientConnectViewModel, IsDesignTimeCreatable=True}"
             Background="#F4F7FC">

    <UserControl.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#007ACC"/>
        </Style>

        <Style x:Key="LightButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InvertedBooleanToVisibilityConverter x:Key="InvertedBoolToVis"/>
    </UserControl.Resources>

    <Border HorizontalAlignment="Center" 
             VerticalAlignment="Center"
             Background="White"
             CornerRadius="5"
             BorderBrush="#DDD"
             BorderThickness="1"
             Padding="30"
             MinWidth="350">

        <StackPanel Orientation="Vertical">
            <TextBlock Text="Kết nối đến Host" 
                       Foreground="#333" 
                       FontSize="24" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <Grid Margin="0,20,0,0"
                  Visibility="{Binding IsConnecting, Converter={StaticResource InvertedBoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Nhập IP của Host:" 
                           Foreground="#555" 
                           FontSize="14" 
                           Margin="0,0,0,5"/>

                <TextBox Grid.Row="1" x:Name="IpTextBox"
                         Text="{Binding HostIpAddress, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="16"
                         Padding="8"
                         BorderBrush="#CCC"
                         BorderThickness="1"/>

                <Button Grid.Row="2" x:Name="ConnectButton"
                        Content="KẾT NỐI"
                        FontSize="18"
                        Padding="12"
                        Margin="0,20,0,0"
                        Style="{StaticResource AccentButton}"
                        Command="{Binding ConnectCommand}"/>

                <Button Grid.Row="3" x:Name="KeyMappingButton"
                        Content="Cài đặt phím (Mapping)"
                        FontSize="14"
                        Padding="8"
                        Margin="0,10,0,0"
                        Style="{StaticResource LightButton}"
                        Command="{Binding ShowKeyMappingCommand}"/>
            </Grid>

            <StackPanel Margin="0,20,0,0"
                        Visibility="{Binding IsConnecting, Converter={StaticResource BoolToVis}}">

                <ProgressBar IsIndeterminate="True" Height="10" Margin="0,10"/>

                <TextBlock x:Name="StatusTextBlock"
                           Text="{Binding StatusText}"
                           Foreground="#007ACC"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"/>
            </StackPanel>

        </StackPanel>
    </Border>
</UserControl>